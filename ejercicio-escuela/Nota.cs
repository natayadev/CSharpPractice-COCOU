using System;

namespace ejercicio_escuela
{
    public class Nota : IEvaluacion
    {
        var nota = new Random();
 
        for (int i = 0; i< 3; i++) {
            var nota = random.Next(1, 11);
    }
}
}
